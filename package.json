{
  "name": "orkestr",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "packages/targets/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "test": "npm run -ws test",
    "lint": "npm run -ws lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.17"
  }
}
